net/http package: server, client, request, response, mux

linked lists

Bitwise operations

context.Backgound
context package

Что такое MIME?
mime/multipart

init functions

net/http: Context() method for Request struct

httptrace package

http.RoundTripper
http.Transport

functions: variadic argumets/parameters

Collection's memory model (как хранится в памяти?): 
arrays, slices, maps, memory, allocations in memory, new variables, copying

understanding closures

image
image/color
image/gif

паттерны конкурентного программирования
fan-in, fan-out

runtime package

